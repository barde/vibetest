# Azure Developer CLI configuration file
# https://aka.ms/azure-dev/azure-yaml

name: copilotblazor
metadata:
  template: custom
services:
  infra:
    project: ./Infra
    language: bicep
    host: azure
